FOCUSVERSE PREMIUM WEBSITE REPLACEMENT
Replace index.html, style.css, script.js, version.json and .nojekyll in the existing GitHub Pages repository.
No build step is required. The animated background is generated with Canvas, so no video asset is needed.
version.json controls the release/download links.
